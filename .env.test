# ============ 测试环境 ============
# npm run dev:test / npm run build:test 加载（等价于 vite --mode test）

VITE_APP_ENV=test
VITE_APP_TITLE=测试环境

# 浏览器端请求的 API 前缀，一般保持 /api
VITE_API_BASE_URL=/api

# 测试环境后端服务地址（域名或 IP）
VITE_API_TARGET=http://test-api.example.com:8888
